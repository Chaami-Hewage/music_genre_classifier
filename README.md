# music_genre_classifier
Web-based song lyrics genre classifier (8 classes) using Spark MLlib + PySpark, with an interactive Chart.js bar chart showing scores for all genres. Includes a one-click run.bat launcher.
